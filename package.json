{
  "name": "btw-audit-checklist",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "config": {
    "allow-scripts": {
      "esbuild": true
    }
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
